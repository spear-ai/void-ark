# Python Base

This package serves as:
- A template for other Spear AI projects written in Python
- (Temporary) A project we can use as a package repository and development environment on the high side.

## Quick Commands

```sh
uv add package_name # Add package to repo
uv run command_name # Run command within virtualenv context

docker build .      # Build Docker image
```
